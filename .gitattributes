# Ensure shell scripts use Unix line endings
*.sh text eol=lf

# Ensure Windows batch files use Windows line endings
*.bat text eol=crlf
*.ps1 text eol=crlf

# Auto-detect text files and normalize line endings
* text=auto